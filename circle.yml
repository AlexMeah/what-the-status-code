machine:
  node:
    version: 4

deployment:
  production:
    branch: master
    commands:
        - npm run build
        - git add -A
        - git commit -m "Deploy"
        - git subtree push --prefix dist origin gh-pages
